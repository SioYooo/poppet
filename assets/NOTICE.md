# Artwork licensing notice

The artwork files under `assets/characters/default/`, the tray icon
`assets/tray-fallback.png`, and the reference PNGs at the repository root are
**not** covered by the MIT code license in `LICENSE`. They are governed by the
repository's separate artwork licensing records, not by MIT: the exact
per-file status of each image — most under the Poppet-scoped artwork licence
(`LicenseRef-Poppet-Scoped-Artwork`) — is recorded in `ASSETS_LICENSE.md` and
`docs/default-character-provenance.md`.

Extracting or reusing these images outside Poppet is not permitted.
